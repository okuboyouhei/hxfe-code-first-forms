# HXFE 月次セキュリティチェックリスト

> **使い方:** 毎月1回、このMDをHXFEのコードベースと一緒にAIエージェント（Claude等）に渡し、「このチェックリストに従って監査を実行し、結果を各項目に記入して返してください」と指示する。結果はHXMDに種別「メモ」カテゴリ「HXシリーズ」でログ化し、次月のレビュー時に前回結果として渡す。

- 実施日: YYYY-MM-DD
- 対象バージョン: HXFE vX.X.X
- 実施者: （人間の確認者名）
- 前回実施日: YYYY-MM-DD

---

## 1. 依存ライブラリの鮮度

HXFEがバンドルしている外部ライブラリは **htmx のみ**（Alpine.jsは不使用。フロントは自前のバニラJS）。

| 項目 | 確認方法 | 結果 | 対応要否 |
|---|---|---|---|
| htmx最新版確認 | Web検索「htmx latest version」→ バンドル版（assets/js/htmx.min.js）と比較 | | |
| htmx既知脆弱性（CVE）確認 | Web検索「htmx CVE」で当年の報告を確認 | | |
| 自前JSのレビュー | hxfe-front.js / hxfe-conditions.js / hxfe-chatbot.js の前回以降のdiffを確認 | | |

**判断基準:** パッチバージョン差は経過観察可。マイナーバージョン差またはセキュリティ修正を含む場合は更新リリースを計画する。

## 2. 入力サニタイズ監査（AIレビュー項目）

コードベースを読み、以下を確認して問題箇所をファイル名・行番号付きで列挙する。

- [ ] `$_POST` / `$_GET` / `$_REQUEST` の直接使用箇所すべてに `wp_unslash()` + `sanitize_*()` が適用されているか
- [ ] 新規追加されたフィールドタイプ（前回レビュー以降のdiff）のサニタイズ・エスケープ
- [ ] `echo` 出力箇所すべての `esc_html()` / `esc_attr()` / `esc_url()` 適用
- [ ] `hxfe_after_submit` フック経由で外部に渡る `$values` がサニタイズ済みであること（連携プラグインへの汚染防止）

## 3. フォーム固有の攻撃面

- [ ] **ファイルアップロード** (`includes/file-upload.php`): 許可拡張子リスト方式か、MIMEタイプ検証があるか、アップロード先のPHP実行が防止されているか
- [ ] **メールヘッダインジェクション** (`includes/mailer.php`): 件名・宛先に改行文字が混入できないか
- [ ] **CAPTCHA迂回** (`recaptcha.php` / `turnstile.php`): 検証失敗時に処理が確実に中断されるか、検証がvalidateHandler側で行われているか（v1.4.4の修正が維持されているか）
- [ ] **ハニーポット** (`honeypot.php`): フィールド名が推測されにくいか
- [ ] **Webhook** (`webhook.php`): 送信先URLの検証、SSRFにつながる内部IP指定が可能でないか
- [ ] **アクセス制御** (`access-control.php`): 認可チェックの漏れ

## 4. nonce / 権限チェック

- [ ] 全Ajaxエンドポイントに `check_ajax_referer()` があるか
- [ ] 全admin-postハンドラに `check_admin_referer()` があるか
- [ ] 設定変更系に `current_user_can()` があるか
- [ ] 前回レビュー以降に追加されたエンドポイント（git logで特定）の検証

## 5. 自動ツール実行

- [ ] Plugin Check実行 → ERROR 0 / WARNING一覧を記録
- [ ] `WP_DEBUG = true` でフォーム送信一連（通常・確認画面・マルチステップ）を実行しnotice/warningゼロを確認
- [ ] 最新WordPressバージョンでの動作確認（readme.txtの `Tested up to` 更新要否）

## 6. 設定値・シークレットの扱い

- [ ] reCAPTCHA/Turnstileのシークレットキーがログ・エクスポート・エラーメッセージに漏れていないか
- [ ] SMTP認証情報（`smtp.php`）の保存方法と出力箇所の確認
- [ ] `logger.php` のログに個人情報・認証情報が平文で残っていないか

## 7. 前回からの差分レビュー

```bash
# 前回レビュー時のタグとの差分を取得してAIに渡す
git diff <前回タグ>..HEAD --stat
git diff <前回タグ>..HEAD -- includes/
```

- [ ] 差分に対する重点レビュー（新規コードは全項目を適用）

---

## 実施結果サマリー（AIが記入）

- 検出した問題: （件数と重要度）
- 対応が必要な項目:
- 経過観察の項目:
- 次回への申し送り:

## HXMDへの記録

このサマリーをHXMDに登録する（種別: メモ / カテゴリ: HXシリーズ / 件名: 「HXFE月次セキュリティレビュー YYYY-MM」）。対応が必要な項目は優先度・期限日を設定した個別ログとして起票する。
